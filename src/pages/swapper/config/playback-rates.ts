/** 再生速度のプリセット。直接入力でこれ以外の値も入れられる。 */
export const PLAYBACK_RATES = [0.5, 0.75, 1, 1.25, 1.5, 2] as const

export const DEFAULT_PLAYBACK_RATE = 1

export const MIN_PLAYBACK_RATE = 0.25
export const MAX_PLAYBACK_RATE = 4
